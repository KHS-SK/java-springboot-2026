<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.pknu26.food.mapper.FoodMapper">

    <select id="findAll" resultType="com.pknu26.food.dto.Food">
        SELECT ID,
               NAME,
               CATEGORY,
               RATING,
               MEMO,
               EAT_DATE,
               CREATED_AT
        FROM FOOD
        ORDER BY ID DESC
    </select>

    <select id="findById" parameterType="long" resultType="com.pknu26.food.dto.Food">
        SELECT ID,
               NAME,
               CATEGORY,
               RATING,
               MEMO,
               EAT_DATE,
               CREATED_AT
        FROM FOOD
        WHERE ID = #{id}
    </select>

    <insert id="insert" parameterType="com.pknu26.food.dto.Food">
        INSERT INTO FOOD (
            ID, NAME, CATEGORY, RATING, MEMO, EAT_DATE
        ) VALUES (
            food_seq.nextval,
            #{name},
            #{category},
            #{rating},
            #{memo},
            #{eatDate}
        )
    </insert>

</mapper>